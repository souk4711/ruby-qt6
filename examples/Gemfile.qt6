# frozen_string_literal: true

source "https://rubygems.org"

# ruby-qt6
%w[qtcore qtgui qtwidgets qtqml qtquick qtquickcontrols2 qtquickwidgets qtuitools qt].each do |lib|
  if lib == "qt"
    gem "ruby-qt6", path: "../#{lib}"
  else
    gem "ruby-qt6-#{lib}", path: "../#{lib}"
  end
end
