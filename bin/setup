#!/usr/bin/env bash


set -euo pipefail


bundle_install() {
  echo "cd $1 && bundle install"
  (cd "$1" && bundle install)
  echo ""
}


bundle_install "."
bundle_install "rice"
for lib in qt[a-z]*; do bundle_install "$lib"; done
bundle_install "qt"
