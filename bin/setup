#!/usr/bin/env bash


set -euo pipefail


bundle_install() {
  echo "cd $1 && bundle install"
  (cd "$1" && bundle install)
  echo ""
}


for lib in qt[a-z]*; do
  bundle_install "$lib"
done
bundle_install "qt"
bundle_install "."
